#include <stdio.h>
int main(){
    int num[9];
    int max=0;
    int s=0;

    for(int i=0; i<9; i++){
        scanf("%d",&num[i]);
    }
    max=num[0];
    for(int i=1; i<9; i++){
        if(num[i]>max){
            max=num[i];
            s=i;
        }
    }
    printf("%d\n%d",max,s+1);
}
